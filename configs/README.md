# Configuration Layout

This folder stores versioned configuration files for data paths, feature windows,
model parameters, and experiment settings.

- `paths.yaml`: project-level filesystem locations
- `data.yaml`: data source names and entity keys
- `models.yaml`: module-by-module modeling defaults
- `base.yaml`: shared runtime defaults
